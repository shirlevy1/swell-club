# הקמה

מדריך צעד־אחר־צעד מרגע שיש חשבונות ועד אפליקציה חיה.
העלויות והחשבונות עצמם: [ACCOUNTS.md](ACCOUNTS.md).

---

## 1. פרויקט Supabase

1. supabase.com → **New project**
2. אזור: **Frankfurt (eu-central-1)** — הכי קרוב לישראל
3. לשמור את סיסמת מסד הנתונים במקום בטוח

## 2. הרצת הסכמה

Supabase → **SQL Editor** → **New query** → להדביק את כל התוכן של
`supabase/migrations/0001_init.sql` → **Run**.

זה יוצר את הטבלאות, את כללי ההרשאות, את פונקציית הצ׳ק־אין, את דלי
האחסון לסלפים, ואת קהילת Swell עצמה.

✅ לוודא: **Table Editor** מראה 6 טבלאות, ו־**Storage** מראה דלי בשם
`selfies` שאינו ציבורי.

## 3. הגדרות התחברות

**Authentication → Sign In / Providers → Email**

| הגדרה | ערך | למה |
|---|---|---|
| Enable Email provider | ✅ | |
| **Confirm email** | ❌ **כבוי** | אחרת כל מצטרף חייב לפתוח מייל לפני שהוא נכנס. חיכוך מיותר לקהילה קטנה — והאימייל עדיין משמש לשחזור סיסמה |

**Authentication → Policies** (מה שאי אפשר לקבוע ב־SQL):

| הגדרה | ערך | למה |
|---|---|---|
| Minimum password length | **8** | הממשק בודק 8 תווים, אבל ברירת המחדל בשרת היא 6. בלי זה אפשר לעקוף את הבדיקה בקריאה ישירה ל-API |
| Leaked password protection | ✅ | חוסם סיסמאות שכבר הודלפו. חינם, ומכבה את התקיפה הכי נפוצה על חשבונות |
| Rate limits | ברירת מחדל | להשאיר. זה מה שמגן על מסך ההתחברות מניחוש סיסמאות |

⚠️ **Confirm email כבוי פירושו שהאימייל אינו מאומת.** אפשר להירשם עם
כתובת של מישהו אחר. בקהילה סגורה שמכירה אחד את השני זה מקובל, וזה
המחיר של הוויתור המודע על SMS — אבל אם הקהילה גדלה מעבר למי שמכירים
אישית, זו ההגדרה הראשונה שצריך להפוך.

**Authentication → URL Configuration** — להוסיף ל־Redirect URLs:

```
http://localhost:3000/**
https://<הדומיין-שלכם>/**
```

בלי זה קישור איפוס הסיסמה יישבר.

## 4. חיבור מקומי

מ־**Project Settings → API** להעתיק שני ערכים לקובץ `.env.local` בשורש
הפרויקט:

```
NEXT_PUBLIC_SUPABASE_URL=https://xxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGci...
```

ואז:

```bash
npm run dev
```

⚠️ `.env.local` כבר ב־`.gitignore`. ה־`service_role key` לא נדרש לאפליקציה
הזו כלל — אין צורך להעתיק אותו לשום מקום.

## 5. מנהלת הקהילה

**מי שנרשם ראשון לקהילה הופך אוטומטית למנהלת שלה.** זה קורה בשרת
(בפונקציה `handle_new_user()`), בלי שאילתה ידנית. **חשוב: תבקשו משיר
שהיא תהיה האדם הראשון שנרשם**, לפני שאתם שולחים את הקישור לקהילה
לאף אחד אחר. מאותו רגע היא רואה את לשונית **ניהול** ויכולה לפתוח מפגשים.

כל מי שנרשם אחריה מקבל אוטומטית `member` — התצוגה שלו לא כוללת את
לשונית הניהול, וגם ה־RLS במסד חוסם ממנו פעולות ניהול גם אם ינסה
לקרוא ל־API ישירות.

אם צריך לקדם מנהל נוסף בהמשך (למשל שיר רוצה שותפה לניהול), ב־SQL
Editor:

```sql
update public.club_members
set role = 'organizer'
where profile_id = (
  select id from auth.users where email = 'האימייל-של-האדם'
);
```

## 6. העלאה לאוויר

1. להעלות את הקוד ל־GitHub
2. vercel.com → **Add New → Project** → לבחור את המאגר
3. תחת **Environment Variables** להדביק את אותם שני משתנים מסעיף 4
4. **Deploy**

אחרי הפריסה לחזור לסעיף 3 ולהוסיף את כתובת הפרודקשן ל־Redirect URLs.

---

## בדיקת השטח — לא לדלג

הצ׳ק־אין הוא החלק היחיד שלא ניתן לבדוק מהשולחן. **לפני מפגש עם אנשים
אמיתיים**, ללכת לחוף ולעבור את הזרימה מהטלפון:

- [ ] המצלמה נפתחת, והיא הקדמית
- [ ] הרשאת המיקום נענית, והמרחק שמתקבל הגיוני
- [ ] העלאת הסלפי מצליחה **על רשת סלולרית**, לא על wifi
- [ ] צ׳ק־אין מחוץ לרדיוס באמת נחסם
- [ ] הרשימה נפתחת מיד אחרי צ׳ק־אין מוצלח
- [ ] הכל עובד גם כשהאפליקציה שמורה למסך הבית (הרשאות נשאלות מחדש בהקשר הזה)

## מה שהמסד מגן עליו — ומה שלא

נאכף בשרת, ולכן אי אפשר לעקוף מהדפדפן:

- **צ׳ק־אין** — זמן, מרחק, ו**קיום הסלפי** נבדקים ב־`check_in()`. הנתיב נגזר מ־`auth.uid()` ולא מתקבל מהלקוח, ולכן אי אפשר לסמן נוכחות בשם מישהו אחר, להצביע על התמונה של מישהו אחר, או לוותר על התמונה.
- **רשימת המשתתפים** — RLS על `attendances`, ומדיניות נפרדת על הקבצים עצמם ב-storage.
- **`attendances` היא לקריאה בלבד ללקוח.** אין policy של INSERT/UPDATE/DELETE. הכניסה היחידה היא דרך הפונקציות.
- **הפונקציות סגורות בפני anon** — `revoke execute … from public, anon` בסוף המיגרציה.

מה שנשאר פתוח, ביודעין:

- **אפשר לזייף מיקום.** דפדפן מאפשר להזריק קואורדינטות (DevTools, תוסף). אין לזה פתרון בלי אפליקציה נייטיבית עם attestation. מה שמגן בפועל: הסלפי מגיע לרשימה שכל המשתתפים רואים, וקהילה קטנה מזהה מיד מי לא היה שם. **זו הגנה חברתית, לא קריפטוגרפית** — ואם המערכת אי פעם תשמש למשהו עם ערך כספי, זה לא יספיק.
- **קישור חתום לסלפי תקף שעה.** מי שמקבל את הכתובת רואה את התמונה עד שהיא פגה, גם בלי להתחבר.
- **הצטרפות פתוחה** — כל מי שיש לו את הכתובת נרשם ונכנס לקהילה. זו החלטת מוצר, לא פרצה, אבל שווה לזכור אותה אם הקהילה גדלה.
- **בלי `.env.local` האפליקציה עולה במצב הדגמה.** מקומית זו ההתנהגות הרצויה; בענן היא נחסמת — דיפלוי בלי המפתחות **נכשל** עם הודעה מפורשת, ולא מגיש אתר דמה שנראה תקין.
- **להעלות את ההדגמה לאוויר בכוונה:** `NEXT_PUBLIC_SWELL_DEMO=1`. גובר גם כשיש מפתחות, ולכן אין צורך למחוק אותם ולהדביק מחדש. מסירים את המשתנה — והמוצר האמיתי חוזר.

## הפעלת תזכורות דחיפה

הקוד בנוי ומחכה לארבעה משתני סביבה. **בלעדיהם הכרטיס "תזכורות" פשוט
לא מוצג** — שום דבר לא נשבר.

**1. לייצר מפתחות** (פעם אחת):

```bash
npm run vapid
```

הפלט כולל גם `PUSH_CRON_SECRET` אקראי. להעתיק הכל ל-Railway → Variables.

**2. להוסיף את מפתח־העל של Supabase** — Project Settings → API →
`service_role`. הוא נדרש **רק** לשליחה, כי ל-cron אין משתמש מחובר ולכן
RLS היה חוסם אותו.

⚠️ המפתח הזה עוקף את כל ההרשאות. **בלי `NEXT_PUBLIC_`** — כל משתנה עם
הקידומת הזאת נשלח לדפדפן, וזו הייתה דליפה מלאה של המסד.

**3. לחבר מתזמן.** להריץ כל 30 דקות:

```bash
curl -X POST https://<הדומיין>/api/push/send -H "x-swell-cron: <PUSH_CRON_SECRET>"
```

ב-Railway זה Cron Service; אפשר גם GitHub Actions. שתי תזכורות לכל
מפגש — כ-12 שעות לפני וכשעה לפני — וטבלת `event_reminders` מונעת כפילות
גם אם המתזמן ירוץ פעמיים.

⚠️ **באייפון התראות עובדות רק באפליקציה ששמורה למסך הבית** (מ-iOS 16.4).
בספארי רגיל ה-API לא קיים כלל — הכרטיס מסביר את זה במקום להיכשל בשקט.

## בדיקת ניגודיות אחרי שינוי צבע

המסך הזה נקרא בשמש ישירה. כל צבע טקסט חייב לעמוד ב־4.5:1 מול הרקע
שמתחתיו — **וזה לא תמיד לבן**: יש גם `page` ו־`haze`. להדביק בקונסול
של הדפדפן, בכל עמוד שנגעתם בו:

```js
(()=>{const lum=c=>{const[r,g,b]=c.match(/\d+/g).map(Number).slice(0,3).map(v=>{v/=255;return v<=0.03928?v/12.92:Math.pow((v+0.055)/1.055,2.4)});return 0.2126*r+0.7152*g+0.0722*b};
const bg=e=>{while(e){const c=getComputedStyle(e).backgroundColor;if(c&&!/rgba\(0, 0, 0, 0\)|transparent/.test(c))return c;e=e.parentElement}return 'rgb(255,255,255)'};
const bad=[];document.querySelectorAll('*').forEach(el=>{const t=[...el.childNodes].filter(n=>n.nodeType===3&&n.textContent.trim()).length;if(!t)return;const s=getComputedStyle(el);if(s.display==='none'||s.visibility==='hidden')return;
const a=lum(s.color),b=lum(bg(el)),r=(Math.max(a,b)+.05)/(Math.min(a,b)+.05),px=parseFloat(s.fontSize),need=(px>=24||(px>=18.66&&+s.fontWeight>=700))?3:4.5;
if(r<need)bad.push(el.textContent.trim().slice(0,30)+' → '+r.toFixed(2))});console.log(bad.length?bad:'תקין')})()
```

## אימות ה־SQL אחרי שינויים

```bash
python3 scripts/check_sql.py
```

בודק תחביר, כולל הלוגיקה שבתוך גופי פונקציות plpgsql. לא מחליף הרצה
אמיתית, אבל תופס טעויות הקלדה לפני שמדביקים ל־Supabase.
