# Swell — מפרט מוצר ותוכנית בנייה

> מסמך זה נגזר מהשיחה בין לירון לשיר לוי. כל דרישה כאן מקורה בשיחה; החלטות שנוספו על ידינו מסומנות כ־**[הצעה]**, ודברים שנשארו פתוחים מרוכזים בסוף.

---

## 1. תקציר מנהלים

אנחנו בונים **אפליקציית ווב (PWA)** לקהילת השחיינים של Swell, שפותרת בעיה אחת חדה:
**אין דרך לדעת מי באמת היה במפגש.**

הפתרון: אי אפשר "להגיד" שהיית במפגש — אתה **מוכיח** את זה בשתי ורפיקציות בו־זמנית:

1. **נוכחות פיזית** — אתה נמצא במקום הנכון בזמן הנכון (geofence + חלון זמן).
2. **תיעוד פנים** — אתה מצלם סלפי חי, בתוך האירוע.

התמורה שגורמת לאנשים לטרוח: **רק מי שנכח ואומת נחשף לרשימת המשתתפים** — עם הסלפי שלהם מאותו מפגש, השם, והאינסטגרם. מי שלא עשה צ'ק־אין לא רואה כלום.

Swell היא הקהילה הראשונה. אם זה עובד — אותו מוצר בדיוק משרת כל קהילה ואירוע קהילתי אחר.

---

## 2. הבעיה (מהשיחה)

**עבור המארגנת (שיר):**
> "אני לא יודעת מי הם אותם 34 אנשים שהגיעו, מעבר לתמונה שיש לי שלהם."

אחרי כל מפגש נשאר בלגן: מי הגיע בפועל, מה המספר טלפון, מה האינסטגרם. אין דאטה, אין המשכיות, אין דרך למדוד כלום.

**עבור המשתתף:**
> "מי זה הבחור שעומד מולי? חברה שלי מעוניינת בו."

אתה יוצא ממפגש עם עשרות אנשים שראית ולא הכרת. הקשרים היחידים שנשארים הם אלה שהחלפת איתם מספר במקום. שאר החדר נעלם.

**למה הפתרונות הקיימים לא עובדים:**

| פתרון קיים | למה נפסל |
|---|---|
| סקר בוואטסאפ | מי שסימן ✅ לא בהכרח הגיע. אין אימות. |
| טופס גוגל (הפתרון הנוכחי, ~60 רשומים) | רישום מראש בלבד. לא יודע מי הגיע בפועל. |
| מישהי שרושמת שמות בכניסה | ידני, לא סקיילבילי, "הכיוון הכי דוות" |
| חלוקת קודי QR במפגש | *"אני לא רוצה בזמן מפגש ללכת כמו דיילת ולחלק QR־ים לאנשים"* |

**ההשראה:** מיוויזיט — מזמינים תור מהבית, וכשמגיעים למרחב הגיאוגרפי של המקום אפשר לסמן שהגעת. אם אתה רחוק מדי — המערכת לא נותנת לך לסמן.

---

## 3. המשתמשים

**המארגנת** (שיר, ובהמשך מארגנים אחרים)
פותחת מפגשים, רוצה לדעת מי נרשם, מי הגיע, ומי חוזר. רוצה שהקהילה תישאר ותגדל.

**חבר הקהילה** (השחיין)
רוצה לדעת מתי המפגש הבא, לסמן שהוא מגיע, ואחרי המפגש — לגלות מי היו האנשים שהיו שם איתו ולהתחבר אליהם.

---

## 4. עקרונות מוצר

אלה ההחלטות שהתקבלו בשיחה ואסור לוותר עליהן — הן המוצר עצמו:

1. **נוכחות מוכחת, לא מוצהרת.** אין כפתור "הייתי כאן". יש הוכחה כפולה.
2. **הסלפי מצולם בזמן אמת, בתוך האירוע.** לא העלאה מהגלריה, לא תמונת פרופיל ישנה.
   > *"אני לובשת חולצה אדומה ולבנה משבצות — הסלפי שאצלם יראה את החולצה, בדיוק איך נראיתי במפגש. וזה עוזר לאנשים לזהות."*
   השורש: תמונת פרופיל שרירותית לא מאפשרת לזהות בן אדם. *"הוא שם תמונה של הגב שלו — לא זיהיתי שזה הוא."*
3. **הרשימה היא התמורה.** גישה לרשימת המשתתפים ניתנת **אך ורק** למי שנכח ואומת באותו מפגש. זה מה שגורם לאנשים לעשות צ'ק־אין.
4. **ווב, לא אפסטור.** אנשים לא אוהבים להוריד אפליקציות ולהתעסק עם חנות. שולחים קישור, נכנסים מהטלפון, שומרים אייקון למסך הבית.
   > שיר: *"זה מושלם, זה מה שהתכוונתי."*
5. **אותנטיות מעל ליטוש.** התמונות אמיתיות ולא מוחמאות, וזה בכוונה. הן נראות רק בתוך המפגש שבו צולמו.
6. **מיקוד.** לירון בשיחה, שלוש פעמים: *"לאט לאט, בוא נתרכז."* כל מה שלא בשלב הנוכחי — נדחה במפורש.

---

## 5. גבולות גזרה

### בפנים (MVP)
הצטרפות לקהילה, פרופיל, יצירת מפגשים, RSVP, צ'ק־אין מאומת, חשיפת רשימת המשתתפים, דשבורד מארגן.

### בחוץ — נדחה במפורש בשיחה
| מה | מתי חוזרים לזה |
|---|---|
| העלאת נתוני שעון מהשחייה (דופק, מרחק) | שיר עצמה סימנה: *"זה כבר התפזרות"* |
| חתונות ואירועים פרטיים | *"בצורה הכי מוגזמת בעולם"* — מאוחר |
| ריבוי קהילות בממשק | המודל תומך מהיום הראשון, הממשק — אחרי ש־Swell עובדת |
| אפליקציה נייטיב באפסטור | לא בתוכנית בכלל |

### הערה על WhatsApp/אינסטגרם בכרטיס
לירון הגדיר את זה "השלב הבא". בפועל אלה שתי שורות קוד (`wa.me/972…` ו־`instagram.com/…`) והן **הדבר שאנשים באמת רוצים** מהרשימה. **[הצעה]** לכלול כבר ב־MVP.

---

## 6. מפרט הפיצ'רים

### 6.1 הצטרפות לקהילה

שיר שולחת קישור. הנכנס נרשם ב־**אימייל וסיסמה**. ההצטרפות פתוחה — כל מי שיש לו קישור נכנס.

> **החלטה:** נשקל אימות בקוד SMS ונפסל — הוא דורש חשבון Twilio והרשמת שולח מול גורמי הסלולר בישראל, וזו בירוקרטיה שלא שווה את התמורה בשלב הזה.
>
> **למה אימייל ולא שם משתמש:** הדרישה היא שמשתמש לא יצטרך להיזכר בפרטים שלו. אימייל פותר את זה טוב יותר משם משתמש בשתי דרכים — הוא **לא ניתן לשכחה** (לכל אחד יש אימייל אחד או שניים, בעוד ששם משתמש הוא המצאה חדשה שנשכחת), והוא נותן **שחזור סיסמה עצמאי**. עם שם משתמש בלבד אין ערוץ שחזור בכלל: מי ששכח סיסמה תקוע, ושיר צריכה לאפס לו ידנית.
>
> **בפועל המשתמש לא יתחבר יותר מפעם אחת:** הסשן נשמר במכשיר לצמיתות, ושדות הטופס יסומנו כך שמנהל הסיסמאות של האייפון יציע לשמור ולמלא אוטומטית.

⚠️ באייפון, אפליקציה ששמורה במסך הבית מקבלת אחסון נפרד מספארי. לכן מסך ההדרכה יורה "הוסף למסך הבית" **לפני** ההרשמה — אחרת המשתמש יצטרך להתחבר פעמיים.

שדות פרופיל:

| שדה | חובה | הערה |
|---|---|---|
| אימייל | ✅ | משמש להתחברות ולשחזור סיסמה |
| סיסמה | ✅ | מינימום 8 תווים |
| שם מלא בעברית | ✅ | |
| מספר טלפון | ✅ | מוקלד ידנית. ⚠️ **לא מאומת** — זו העלות של הוויתור על SMS. נאמת פורמט ישראלי בלבד |
| אינסטגרם | ➖ | שדה חופשי. בטופס הקיים חלק כתבו handle וחלק הדביקו קישור — נקבל את שניהם ונרמל בצד שלנו |
| תמונת פרופיל | ➖ | לא נבקש בהרשמה. הסלפי הראשון מהמפגש יהפוך לתמונה — נאמן לעיקרון האותנטיות ומוריד חיכוך |

לאחר ההצטרפות נפתח "עולם הקהילה" — המפגשים.

**~60 הרשומים הקיימים בטופס הגוגל:** נטען מראש לטבלת `known_members`. כשמישהו מהם מתחבר, הפרטים שכבר מסר יופיעו ממולאים.

### 6.2 מפגשים

מפגש = **זמן ומקום**. זה הבסיס.

המארגנת יוצרת: כותרת, תאריך ושעה, מיקום (נעיצה על מפה → `lat/lng`), שם מקום ("החוף", "החניה"), רדיוס צ'ק־אין, וחלון הצ'ק־אין.

ברירות מחדל: רדיוס **150 מ'**, חלון **15 דק' לפני עד 15 דק' אחרי** — ניתן לשינוי לכל מפגש (*"תלוי מה האירוע, אז נחליט"*).

תצוגה למשתתף: **[הצעה]** רשימה כרונולוגית. מפה נשקלה בשיחה ולא הוכרעה — במפגש שבועי אחד באותו חוף היא לא מוסיפה מידע.

### 6.3 RSVP — "מתכוון להגיע"

כפתור אחד. אופציה להוסיף ליומן (קובץ `.ics`). המארגנת רואה מיד מי מתכנן להגיע.

**החלטה (עודכנה):** משתתף רואה **שמות של מי שסימן שיגיע** — ברשימת המפגשים ("בדרך: נועה ברק, מאיה אלון ועוד 4") ובעמוד המפגש עצמו. **בלי תמונות.**

הנימוק לשינוי: מספר יבש לא מזיז אף אחד בשש בבוקר. מה שמזיז זה לראות ששניים־שלושה שאתם מכירים כבר סימנו. זו בדיוק המוטיבציה שהמוצר צריך לייצר.

מה **לא** השתנה — וזה העיקר: הצהרת כוונה היא מה שהאדם בחר לפרסם, ואילו **הסלפי נוצר רק בצ׳ק־אין מאומת**. הפנים נשארות הפרס על נוכחות מוכחת, ורואים אותן רק אחרי צ׳ק־אין לאותו מפגש. לכן `event_going_list()` מחזירה שם בלבד ולא `selfie_path`, וברשימת הכוונות מוצגת אות אחת מהשם במקום תמונה.

### 6.4 צ'ק־אין — לב המוצר

חלון הצ'ק־אין נפתח 15 דק' לפני. עד אז הכפתור נעול ומראה טיימר.

```
1. לחיצה על "הגעתי"
2. בקשת הרשאת מיקום  →  שליחת lat/lng + דיוק
3. פתיחת מצלמה קדמית  →  צילום סלפי  →  דחיסה מקומית
4. שליחה לשרת
5. השרת מאמת:  בתוך חלון הזמן?  בתוך הרדיוס?  לא צ'ק־אין כפול?
6. ✅ נכחת — הרשימה נפתחת
```

**האימות חייב לרוץ בשרת.** ולידציה בדפדפן = כל אחד יכול לזייף. הלוגיקה יושבת בפונקציית Postgres (סעיף 8.2).

מצבי שגיאה, בעברית ברורה: `אתה רחוק מדי מהמפגש` · `הצ'ק־אין נפתח ב־6:30` · `הצ'ק־אין נסגר` · `כבר עשית צ'ק־אין`.

### 6.5 הרשימה — התמורה

מיד אחרי צ'ק־אין מוצלח: גריד של כל מי שנכח.

כל כרטיס: **הסלפי מאותו מפגש** · שם מלא · אינסטגרם ↗ · WhatsApp ↗

הכלל שמחזיק את הכל: **רואים את הרשימה של מפגש X רק אם עשית צ'ק־אין למפגש X.** נאכף ברמת מסד הנתונים, לא ברמת הממשק (סעיף 8.3).

### 6.6 דשבורד מארגן

לכל מפגש: כמה סימנו שיגיעו · כמה הגיעו בפועל · הפער ביניהם.
לכל חבר: לכמה מפגשים הגיע · אחוז הגעה · מתי היה לאחרונה.
ייצוא CSV. **[הצעה]** הוספה ידנית של משתתף — למי שהטלפון מת או שהאיתור נכשל. בלי זה, כשלון טכני אחד = בן אדם שנמחק מההיסטוריה.

---

## 7. ארכיטקטורה טכנית

| רכיב | בחירה | נימוק |
|---|---|---|
| Frontend | **Next.js (App Router) + TypeScript** | PWA, פריסה בדקות, אקוסיסטם בוגר |
| עיצוב | **Tailwind CSS**, `dir="rtl"`, Frank Ruhl Libre + Assistant | RTL נקי דרך utilities לוגיים (`ps-`/`pe-`/`text-start`). הפלטה נגזרת מהלוגו: כחול־ים רך ולבן |
| Backend + DB | **Supabase** (Postgres) | Auth + Storage + RLS בחבילה אחת |
| התחברות | Supabase Auth — אימייל וסיסמה | חינמי ומיידי, בלי תלות בספק SMS |
| מפות | Leaflet + OpenStreetMap | קוד פתוח, בלי חשבון ובלי עלות. מספיק לנעיצת סיכה בחוף |
| אחסון סלפי | Supabase Storage, bucket **פרטי**, signed URLs | סלפי לא יושב בכתובת ציבורית |
| פריסה | **Vercel** | HTTPS אוטומטי — תנאי הכרחי למצלמה ולמיקום |

**PWA:** `manifest` עם `display: standalone`, `apple-touch-icon`, אייקון Swell.
⚠️ ב־iOS אין הצעת התקנה אוטומטית — המשתמש חייב לעשות שיתוף → "הוסף למסך הבית" **ידנית**. נבנה מסך הדרכה קצר שמלמד את זה. זה בדיוק האייקון שהוא הדרישה של שיר, אז זה לא פרט טכני.

**מצלמה:** `getUserMedia({ video: { facingMode: 'user' } })` ולא `<input capture>` — מבטיח מצלמה קדמית, חוסם גישה לגלריה, ונותן שליטה מלאה ב־UX. דחיסה ל־JPEG ~1080px איכות 0.7 לפני העלאה.

---

## 8. מודל הנתונים

### 8.1 טבלאות

```sql
profiles      (id → auth.users, full_name, phone,
               instagram, avatar_path, created_at)

clubs         (id, name, slug, description)         -- ב-MVP: שורה אחת, Swell

club_members  (club_id, profile_id, role, joined_at) -- role: member | organizer

events        (id, club_id, title, starts_at, location_name,
               lat, lng, checkin_radius_m,
               checkin_opens_before_min, checkin_closes_after_min,
               created_by)

rsvps         (event_id, profile_id, status, created_at)  -- status: going | not_going

attendances   (event_id, profile_id, selfie_path, checked_in_at,
               lat, lng, accuracy_m, distance_m)
              -- זו הרשומה היחידה שמעידה על נוכחות אמיתית
```

### 8.2 אימות הצ'ק־אין (בשרת)

```sql
create or replace function check_in(
  p_event_id uuid, p_lat float8, p_lng float8,
  p_accuracy_m float8, p_selfie_path text
) returns attendances
language plpgsql security definer as $$
declare v_event events; v_dist float8; v_row attendances;
begin
  select * into v_event from events where id = p_event_id;
  if not found then raise exception 'EVENT_NOT_FOUND'; end if;

  if now() < v_event.starts_at - make_interval(mins => v_event.checkin_opens_before_min)
  or now() > v_event.starts_at + make_interval(mins => v_event.checkin_closes_after_min)
  then raise exception 'OUTSIDE_TIME_WINDOW'; end if;

  -- Haversine, מטרים
  v_dist := 6371000 * 2 * asin(sqrt(
    power(sin(radians(p_lat - v_event.lat)/2), 2) +
    cos(radians(v_event.lat)) * cos(radians(p_lat)) *
    power(sin(radians(p_lng - v_event.lng)/2), 2)));

  if v_dist > v_event.checkin_radius_m then raise exception 'TOO_FAR'; end if;

  insert into attendances (event_id, profile_id, selfie_path, lat, lng, accuracy_m, distance_m)
  values (p_event_id, auth.uid(), p_selfie_path, p_lat, p_lng, p_accuracy_m, v_dist)
  returning * into v_row;
  return v_row;
end $$;
```

הפונקציה משתמשת ב־`auth.uid()` ולא בפרמטר — אי אפשר לעשות צ'ק־אין בשם מישהו אחר.

### 8.3 כלל הפרטיות המרכזי

```sql
-- helper ב-security definer, כדי למנוע רקורסיית RLS על attendances
create or replace function has_attended(p_event_id uuid) returns boolean
language sql security definer stable as $$
  select exists (select 1 from attendances
                 where event_id = p_event_id and profile_id = auth.uid());
$$;

create policy "attendees see co-attendees" on attendances
for select using ( has_attended(event_id) );

create policy "see co-attendee profiles" on profiles
for select using (
  id = auth.uid()
  or exists (select 1 from attendances a
             where a.profile_id = profiles.id and has_attended(a.event_id))
);
```

⚠️ **מלכודת:** policy על `attendances` שמריצה `select` על `attendances` תיכנס לרקורסיה אינסופית. ה־`security definer` עוקף RLS ופותר את זה. זו טעות ששורפת חצי יום — היא מתועדת כאן מראש.

---

## 9. תוכנית הבנייה

חמישה שלבים. כל שלב מסתיים במשהו שאפשר להפעיל ולראות.

**שלב 0 — תשתית**
Next.js + Tailwind + RTL עברית · פרויקט Supabase + סכמה + RLS · פריסה ל־Vercel · manifest ואייקון
*סיום: שיר מוסיפה את האייקון למסך הבית באייפון שלה ורואה מסך "בקרוב".*

**שלב 1 — קהילה ופרופיל**
התחברות ב־OTP · יצירת פרופיל · הצטרפות דרך קישור · טעינת 60 הרשומים הקיימים
*סיום: אדם אמיתי נכנס מקישור ויוצא עם פרופיל.*

**שלב 2 — מפגשים ו־RSVP**
יצירת מפגש (כולל נעיצה על מפה) · רשימת מפגשים · "מתכוון להגיע" · ייצוא `.ics`
*סיום: שיר פותחת את מפגש יום שני והקהילה מסמנת הגעה.*

**שלב 3 — צ'ק־אין** ⭐ *השלב הקריטי*
חלון זמן · הרשאת מיקום · מצלמה + דחיסה · אימות בשרת · מצבי שגיאה
*סיום: **בדיקת שטח בחוף** — לירון עושה צ'ק־אין אמיתי מהמקום. לא ניתן לדלג על זה.*

**שלב 4 — הרשימה**
גריד המשתתפים · כרטיס עם סלפי, שם, אינסטגרם, WhatsApp · אכיפת RLS
*סיום: **המפגש האמיתי הראשון על המערכת.***

**שלב 5 — דשבורד מארגן**
נרשמו מול הגיעו · סטטיסטיקת חבר · CSV · הוספה ידנית

### סטטוס

**שלבים 0–5 כתובים ועוברים build ו־lint נקיים.** הקוד שלם וממתין רק
לחיבור Supabase — ראו [SETUP.md](SETUP.md).

מה שנבדק ומה שלא:

| | |
|---|---|
| ✅ נבדק ויזואלית | דף הנחיתה, כרטיסי מפגש, רשימת המשתתפים, מצבי השגיאה, כרטיס הצ׳ק־אין (דרך `/preview`, מסך דמה שקיים רק בפיתוח) |
| ✅ אומת תחבירית | כל ה־SQL כולל גופי plpgsql, דרך `scripts/check_sql.py` |
| ⚠️ **לא נבדק בריצה** | כל מה שנוגע במסד הנתונים. אין Docker על המכונה, ולכן לא היה Supabase מקומי להריץ מולו. המסכים שדורשים התחברות לא נראו בפעולה |
| ⚠️ **לא נבדק במציאות** | מצלמה, מיקום, והעלאה ברשת סלולרית. זה מחייב יציאה לחוף |

**מקור הסיכון היחיד ללו"ז הוא שלב 3.** יעד ריאלי: המפגש האמיתי הראשון
על המערכת תוך 2–3 שבועות.

✅ **אין תלויות חיצונות חוסמות.** הוויתור על SMS ביטל את התלות היחידה בגורם שלישי. כל מה שנדרש הוא פתיחת חשבונות Supabase ו־Vercel — עניין של רבע שעה, ראו [ACCOUNTS.md](ACCOUNTS.md).

---

## 10. סיכונים ומלכודות

| סיכון | חומרה | מענה |
|---|---|---|
| **קליטה גרועה בחוף ב־6:45** | 🔴 גבוה | דחיסת תמונה אגרסיבית לפני העלאה, UI אופטימי, ניסיון חוזר אוטומטי. זה הסיכון הכי מעשי בכל הפרויקט |
| **חלון זמן נוקשה נועל אנשים בחוץ** | 🔴 גבוה | המפגש מתחיל באיחור → כולם ננעלו. חלון נדיב + הוספה ידנית ע"י המארגנת |
| **חיכוך הרשאות ב־iOS** | 🟠 בינוני | שתי בקשות הרשאה ברצף, בשמש, תוך 30 שניות. UX חייב להיות חד. חובה לבדוק על האייפון של שיר לפני מפגש אמיתי |
| **פרטיות — סלפי וטלפונים של אנשים אמיתיים** | 🟠 בינוני | bucket פרטי, signed URLs, RLS. **+ שורת הסכמה מפורשת בהרשמה** על מי רואה את התמונה |
| **זיוף מיקום** | 🟢 נמוך | ניתן לזייף במאמץ. מקובל בקהילה ידידותית — לא מוצר אבטחה. נשמור `accuracy_m` ו־`distance_m` כדי לזהות אנומליות |
| **התנגדות לצילום סלפי** | 🟢 נמוך | הועלה בשיחה ונדחה במודע: *"הרבה דברים שלא היו טבעיים פעם הפכו לטבעיים."* נמדוד: אחוז מי שסימן RSVP ולא השלים צ'ק־אין |

---

## 11. החלטות שהתקבלו

| # | נושא | ההחלטה |
|---|---|---|
| 1 | **התחברות** | אימייל וסיסמה. SMS נפסל בגלל בירוקרטיה מול ספקי הסלולר |
| 2 | **חשיפת נרשמים** | שמות של מי שסימן שיגיע — כן, כי זה מה שמייצר מוטיבציה. תמונות — לא. הפנים הן הפרס על נוכחות מוכחת |
| 3 | **הצטרפות** | פתוחה — כל מי שיש לו קישור. הצ'ק־אין המאומת הוא הסינון האמיתי |
| 4 | **תצוגת מפגשים** | רשימה כרונולוגית. מפה לא מוסיפה מידע במפגש שבועי באותו חוף |
| 5 | **סלפי בהרשמה** | לא. הסלפי הראשון מהמפגש הופך לתמונת הפרופיל |
| 6 | **רדיוס וחלון** | 150 מ', 15 דק' לפני ואחרי — ברירת מחדל, תכויל בבדיקת השטח |
| 7 | **תשתית** | Supabase (Frankfurt) + Vercel + GitHub. עלות התחלתית: $0 |

### נותר פתוח
- **שם ודומיין.** מתחילים על כתובת `*.vercel.app` וניתן לחבר דומיין בכל שלב בלי לשנות קוד.
- **כיול רדיוס וחלון הזמן** — דורש יציאה לחוף. חלק משלב 3.
