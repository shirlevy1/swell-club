<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->

# Swell

אפליקציית ווב (PWA) לקהילת שחיינים. המפרט המלא: `SPEC.md`. חשבונות ועלויות: `ACCOUNTS.md`.

## הכלל שמחזיק את כל המוצר

רואים את רשימת המשתתפים של מפגש **רק אם עשית צ'ק־אין מאומת לאותו מפגש**.
נאכף ב־RLS במסד הנתונים — לעולם לא רק בממשק.

צ'ק־אין מאומת = מיקום בתוך הרדיוס **וגם** סלפי חי, **שניהם מאומתים בשרת**.
ולידציה בדפדפן בלבד = כל אחד יכול לזייף, וזה מפרק את המוצר.

## החלטות שהתקבלו — לא לפתוח מחדש בלי לשאול

- **התחברות: אימייל וסיסמה.** SMS נפסל (בירוקרטיה מול ספקי סלולר). מספר הטלפון נאסף כשדה פרופיל אבל **אינו מאומת**.
- **לפני מפגש חושפים שמות של מי שסימן שיגיע, ותמונה רק למי שכבר
  נכחתם איתו יחד בעבר.** הצהרת כוונה מותרת לפרסום ומייצרת את המוטיבציה
  להגיע; הסלפי נוצר רק בצ׳ק־אין מאומת, ולכן **הפנים** הן הפרס על היכרות
  אמיתית — לא על עצם הרישום לקהילה. `event_going_list()` מחזירה
  `selfie_path` (מ-migration 0018), אבל רק אם כבר יש נוכחות משותפת בעבר;
  `events_going_names()` (כרטיסי רשימת המפגשים) עדיין לא מחזירה תמונה
  בכלל — שם מוצג רק טקסט.
- **הצטרפות פתוחה** לכל מי שיש לו קישור.
- **בלי אפסטור.** PWA שנשמרת למסך הבית.
- סלפי לא נדרש בהרשמה — הראשון מהמפגש הופך לתמונת הפרופיל.

## עיצוב

**הפלטה נגזרת מהלוגו: כחול־ים רך ולבן. אין צבע שלישי.**
`sky` (#92adc5) הוא הצבע מהלוגו — דקורטיבי בלבד, בהיר מדי לטקסט.
`sea` (#46738f) הוא הגרסה שעומדת בניגודיות מול לבן — כל הפעולות והקישורים.
ערכת נושא **בהירה**: משטחים לבנים על רקע ערפילי. זו לא רק אסתטיקה —
בשמש חזקה מסך בהיר קריא יותר מכהה שמחזיר השתקפויות.

טוקנים ב־`globals.css` תחת `@theme` — להשתמש בהם, לא בצבעים קשיחים.
גופנים: Frank Ruhl Libre (כותרות) + Assistant (גוף) + Sacramento (הלוגו בלבד).

⚠️ **`swell-logo.tsx` הוא שחזור של הלוגו, לא הקובץ המקורי.** הלטרינג
שם הוא גופן, לא האותיות המצוירות. כשיגיע קובץ מקור (SVG/PNG) — להחליף
את הרכיב בתמונה.

הלוגו מופיע בשני גדלים: מלא עם הכתב (נחיתה, הרשמה), ו־`showWordmark={false}`
— גלים בלבד — ב־`AppHeader` בתוך האפליקציה, כי הכתב לא קריא ב־32px.
**סרגל המותג חייב להיות בשלד `(app)`**: בלעדיו הלוגו נראה רק בדף
הנחיתה, ובמצב הדגמה עוברים אותו בלחיצה אחת ולא חוזרים.

⚠️ מילוי צבע ב־10–12% שקיפות נקרא כמושבת על רקע לבן. לכפתורים משניים
להשתמש ב־`haze` עם מסגרת, ואייקון בצבע מלא.

## עברית

RTL: `dir="rtl"` על `<html>`. utilities לוגיים (`ps-`/`pe-`/`ms-`/`me-`/`text-start`)
ולא `pl-`/`pr-`. מספרים **טהורים** בתוך טקסט עברי — לעטוף ב־`.ltr-nums`.

- **הקהילה היא "שחיינים"**, בשי"ן. השורש הוא ש.ח.ה. "סחיין" אינו מילה.
- **פנייה ברבים** בכל הממשק ("אתם", "נכחתם"), חוץ מכפתור הסימון "אני
  מגיע/ה" עצמו — שם המרחק (מ"סמנו שתגיעו") נשבר במכוון לטובת הזדהות
  אישית. גוף ראשון תמיד עם סיומת מגדרית ("מגיע/ה"), לא "אני מתכוון"
  שמניח זכר.
- **יחיד ורבים.** לא "1 מפגש" אלא "מפגש אחד"; לא "1 מתכוון" אלא
  "אדם אחד מתכוון". יש ענף נפרד ל־0, 1 ולרבים.
- **בלי משכי זמן קשיחים בטקסט.** חלון הצ׳ק־אין ניתן לעריכה בכל מפגש,
  ולכן `formatMinutes()` ולא "רבע שעה" כתוב בקוד.
- **כותרת שתואמת לשעה.** בהדגמה `swimTitleFor()` בוחר בין שחיית
  בוקר/צהריים/שקיעה/לילה — אחרת מוצג "שחיית בוקר" בשש בערב.

מובייל תחילה. משתמשים בזה בחוף, בשמש, בידיים רטובות: ניגודיות גבוהה ומטרות מגע גדולות.

## מפת הקוד

```
supabase/migrations/0001_init.sql   סכמה, RLS, check_in(). מקור האמת של ההרשאות
scripts/check_sql.py                אימות תחביר SQL (אין Postgres מקומי)

src/proxy.ts                        רענון סשן + שמירה על מסלולים
src/lib/supabase/                   לקוחות: דפדפן, שרת, proxy
src/lib/data.ts                     שאילתות צד-שרת (getViewer וכו')
src/lib/geo.ts                      Haversine — תאום של הנוסחה ב-SQL
src/lib/checkin.ts                  חלון הזמן + תרגום שגיאות check_in
src/lib/format.ts                   תאריכים בעברית, טלפון, אינסטגרם

src/app/(auth)/                     הרשמה, התחברות, איפוס סיסמה
src/app/(app)/                      האפליקציה עצמה (מוגן בהתחברות)
src/app/preview/                    מסך דמה לבדיקה ויזואלית. פיתוח בלבד
src/components/check-in-flow.tsx    מיקום → מצלמה → דחיסה → אימות בשרת
src/components/attendee-grid.tsx    הפרס — מי נכח, עם הפנים
src/components/going-list.tsx       מי מתכוון להגיע. שמות בלבד, בלי תמונות
src/app/(app)/people/[id]/          עמוד אדם לחבר קהילה. נעול עד שנכחתם יחד
src/components/selfie-history.tsx  גלריית הסלפים של אדם לאורך זמן
src/lib/streak.ts                   רצף שבועי סלחני. שבוע ולא מפגש
src/app/api/push/send/route.ts      שולח תזכורות. נקרא מ-cron בלבד
public/sw.js                        service worker — התראות בלבד, בלי קאש
```

## מלכודות שכבר שילמנו עליהן

- **רקורסיית RLS.** policy על `attendances` שקוראת מ־`attendances` נכנסת ללולאה. הפתרון: `has_attended()` ב־`security definer`. אותו דבר ב־`club_members`.
- **`.ltr-nums` רק על מספרים טהורים.** על מחרוזת מעורבת כמו "10 באוגוסט 2026" היא הופכת את סדר המילים.
- **הסלפי נשמר ממוראה, כמו בתצוגה המקדימה של המצלמה** (`ctx.scale(-1,1)` על ה-canvas לפני הציור, ב-check-in-flow ובedit-selfie-button). זו בחירה מוצרית מודעת — "רגיל" יותר לעין מהכיוון האמיתי — לא באג. זיהוי הפנים רץ **אחרי** ההיפוך, ולכן מודד ושומר קואורדינטות שכבר מתאימות לתמונה הסופית; אין תיקון נפרד לעשות.
- **נתיב האחסון הוא `{event_id}/{profile_id}.jpg`** ולא משהו אחר. מדיניות ה-storage נשענת עליו כדי לדעת מי רשאי לראות.
- **`middleware.ts` הוחלף ב־`proxy.ts`** ב-Next 16.
- **פונקציה חדשה ב-`public` נגישה ל-anon כברירת מחדל.** Postgres נותן EXECUTE ל-public אוטומטית. בסוף המיגרציה יש בלוק `revoke`/`grant` — **כל פונקציה חדשה צריכה להיכנס אליו.**
- **RLS הוא ברמת השורה, לא העמודה.** `select("*")` על טבלה שמותר לקרוא ממנה שולח את **כל** השדות לדפדפן. בשאילתות על פרופילים של אחרים — עמודות מפורשות.
- **`?next=` הוא קלט של תוקף.** רק מסלול שמתחיל ב־`/` ולא ב־`//`.
- **ייצוא CSV הוא הרצת קוד אצל המנהלת.** תא שמתחיל ב־`= + - @` נפתח באקסל כנוסחה, והשמות מגיעים מטופס ההרשמה. `neutralize()` ב־`export-button` מוסיף גרש.
- **כל צבע טקסט חייב 4.5:1**, ולא רק מול לבן — גם מעל `page` ו־`haze`. הסקריפט בסוף SETUP.md סורק עמוד חי.
- **`sky` לא נושא טקסט, גם לא לבן** (2.3:1). לעיגולים עם אות — `sea`.
- **כל יציאה מ־`step === "uploading"` חייבת להחזיר ל־`idle`.** `return setError(...)` לבדו משאיר כפתור מושבת לנצח, מול משתמש שעומד בחוף.
- **`SUPABASE_SERVICE_ROLE_KEY` בלי `NEXT_PUBLIC_`.** כל משתנה עם הקידומת נשלח לדפדפן, ומפתח־על שם הוא דליפה של כל המסד.
- **בלי קאש ב-service worker.** רשימת משתתפים מקאש היא שקר על מי נמצא בים עכשיו.
- **`Number(form.get(x) ?? 15)` הוא באג.** שדה ריק מחזיר `""` ולא `null`, ו־`Number("")` הוא 0.
- **ראשי תיבות בעברית נקראים כמילה.** "שיר לוי" → "של", "יונתן שגב" → "יש". בעיגולי הכוונות יש אות אחת מהשם הפרטי.
- **`rsvps` נעולה ב-RLS לשורה של עצמך.** כל תצוגה של נרשמים אחרים חייבת לעבור דרך פונקציית `security definer` — אחרת היא תעבוד בהדגמה ותחזיר רשימה ריקה במצב אמיתי.
- **עוגן בתוך עוגן.** שורת חבר בניהול היא `Link` שלם, ולכן האינסטגרם בתוכה הוא טקסט ולא קישור. הקישור החי נמצא בעמוד החבר.

## שני עמודי אדם, ובכוונה

`/people/[id]` הוא הצד של חבר הקהילה: שם תמיד, אבל אינסטגרם ופנים רק
אם **נכחתם יחד** במפגש. `/admin/members/[id]` הוא הצד של המנהלת: טלפון
וכל הסלפים. מנהלת שמגיעה ל־`/people/[id]` מופנית לעמוד שלה — עדיף
הפניה מגרסה מצומצמת שתיראה לה שבורה.

`profiles_select` מרשה לקרוא פרופיל רק של מי שנכחתם איתו, ולכן את השם
לעמוד הזה מביאה `person_card()` ב־`security definer`, ולא הטבלה.

## הסלפים כזהות

כל צ'ק־אין שומר תמונה נפרדת: הנתיב `{event_id}/{profile_id}.jpg` ייחודי
למפגש, ולכן **ההיסטוריה נשמרת מעצמה** — לא דורסים תמונה קודמת. זה הלב
של הזיהוי: לא תמונת פרופיל אחת שהתיישנה, אלא איך הבן־אדם נראה בכל מפגש.

מי רואה מה:
- **חבר קהילה** — הסלפים של עצמו, ושל מי שנכח איתו באותו מפגש.
- **מנהלת** — הכל, גם ממפגשים שהיא לא נכחה בהם. זה מה ש־`is_event_organizer()`
  פותח במדיניות ה-storage. בלעדיו היא רואה רשימת שמות בלי פנים.

## מצב הדגמה

בלי `.env.local` האפליקציה עולה במצב הדגמה: `demoMode = !supabaseConfigured`
ב־`src/lib/config.ts`. נתונים בזיכרון (`src/lib/demo/store.ts`), מוטציות
דרך server actions (`src/lib/demo/actions.ts`), בלי התחברות.

הענפים יושבים במקום אחד לכל נושא — `src/lib/data.ts` לקריאה, ובכל
רכיב לקוח שכותב (`rsvp-button`, `check-in-flow`, `profile-form`,
`admin/events/new`). **כשמוסיפים פיצ'ר שנוגע בנתונים, צריך את שני
הענפים.**

⚠️ **אסור לקרוא ל־`lib/supabase/server.ts` מתוך עמוד.** כל קריאה מהמסד
עוברת דרך `lib/data.ts`. הפונקציה זורקת שגיאה מפורשת אם קוראים לה
במצב הדגמה — בלי זה עמוד כזה מתפוצץ עם "URL and Key are required",
שגיאה שלא מרמזת על הסיבה. זה כבר קרה פעם אחת ב־`profile/page.tsx`.

⚠️ **בענן, חוסר מפתחות הוא שגיאה ולא ברירת מחדל.** `assertNotAccidentalDemo()`
ב־`config.ts` מזהה פלטפורמת אירוח (Railway/Vercel/Render/Fly) וזורק, כדי
שדיפלוי לא יגיש בשקט אתר דמה בלי התחברות. הדגמה מכוונת: `NEXT_PUBLIC_SWELL_DEMO=1` — **חייב `NEXT_PUBLIC_`**, כי `demoMode` נקרא גם בלקוח. גם ה-proxy בודק אותו, אחרת הוא מפנה הכל ל-/login.
מקומית אין סימן פלטפורמה, ולכן ההדגמה ממשיכה לעבוד כרגיל.

בהדגמה מדולגת בדיקת המיקום בלבד. המצלמה עובדת באמת — זה הרגע שמסביר
את המוצר, ולא כדאי לזייף אותו.

### שני הצדדים: חבר קהילה מול מנהלת קהילה

זו הפרדת הרשאות אמיתית, לא רק כפתור שמוסתר. `role` על `club_members`
הוא `member` או `organizer` — נאכף גם ב־UI (הטאב "ניהול" מוסתר, `/admin`
מפנה חוצה) וגם ב-RLS דרך `is_club_organizer()`.

**במצב אמיתי:** מי שנרשם **ראשון** לקהילה הופך אוטומטית למנהל שלה
(ב־`handle_new_user()`), בלי שאילתת SQL ידנית. מי שמצטרף אחריו מקבל
`member`. ראו SETUP.md סעיף 5.

**בהדגמה:** יש מתג ב־`DemoBadge` (`src/components/demo-badge.tsx`)
שמחליף את `demo.myRole` ב־`store.ts` בין `organizer` ל־`member`, כדי
שאפשר להראות את שני הצדדים בלי לפתוח שני חשבונות. בלעדיו ההדגמה תמיד
מציגה מנהלת — מה שגורם לזה להיראות כמו פלטפורמה אחת בלי הפרדת הרשאות,
וזו בדיוק לא ההתנהגות של המוצר האמיתי.
