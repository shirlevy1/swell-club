-- ============================================================================
-- Swell — עדכון חי של אלבום המפגש
-- ============================================================================
-- בלי זה, אישור תמונה על ידי המנהלת לא מגיע לחברי הקהילה שכבר פתוחים
-- בעמוד המפגש — הם היו צריכים לצאת ולהיכנס מחדש. הוספת הטבלה
-- לפרסום ה-realtime גורמת לשינויים (אישור/מחיקה/העלאה) להישלח מיד
-- לכל מי שמאזין, כפוף לאותו RLS בדיוק שכבר חל על קריאה רגילה.
--
-- להדבקה ב-Supabase → SQL Editor → New query → Run.
-- ============================================================================

alter publication supabase_realtime add table public.event_photos;
