-- ============================================================================
-- Swell — נעילת הרשאות ל-normalize_phone(), כמו לכל פונקציה אחרת
-- ============================================================================
-- normalize_phone() נוצרה ב-0013_unique_phone.sql בלי revoke/grant, בניגוד
-- לכלל הקבוע באתר (ראו AGENTS.md) שכל פונקציה חדשה חייבת חסימה מפורשת —
-- אחרת Postgres נותן EXECUTE לכולם (כולל anon) כברירת מחדל. is_phone_available,
-- שמוגדרת מייד אחריה באותו קובץ, כן טופלה נכון; זו משלימה את אותו טיפול כאן.
--
-- אין כאן שינוי התנהגות אמיתי — הפונקציה לא נוגעת בנתונים, רק מעצבת טקסט —
-- זו סגירת פרצה עתידית, לא תיקון של ניצול קיים.
-- ============================================================================

revoke execute on function public.normalize_phone(text) from public;
grant execute on function public.normalize_phone(text) to anon, authenticated;
